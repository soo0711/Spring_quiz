<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.quiz.lesson02.mapper.StoreMapper">
 	<select id="selectStoreList" resultType="com.quiz.lesson02.domain.Store">
		SELECT
			`id`
			,`name`
			,`phoneNumber`
			,`address`
		FROM
			`store`
 	</select>
 	
 	<select id="selectStoreListById" parameterType="int" resultType="com.quiz.store.domain.Review">
		SELECT
			`id`
			,`storeId`
			,`menu`
			,`userName`
			,`point`
			,`review`
			,`createdAt`
			,`updatedAt`
		FROM
			`new_review`
		WHERE 
			`storeId` =  ${storeId}
 	</select>
 	
 </mapper>
